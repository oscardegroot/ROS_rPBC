
# Network parameters
network:
  enabled: true # Enables artificial effects
  delay: 
    constant: false # Enables delay
    value: 0.1  # Initial delay
    change: 0.3 # (0 - 1) (* sampling period)
    max_delay: 0.15
    min_delay: 0.05
  packet_loss:
    enabled: true # Enables packet loss
    p: 0.02 # 5 percent!